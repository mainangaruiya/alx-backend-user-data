#!/bin/bash

# Add all changes to the Git staging area
git add .

# Commit the changes with a specified commit message
git commit -m 'fixes on errors'

# Push the committed changes to the remote repository
git push
